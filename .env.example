# Use the exact HTTPS domain that the company owns. It is used for canonical URLs,
# sitemap.xml and structured data during the static build.
NEXT_PUBLIC_SITE_URL=

# Optional: use the verified public contact mailbox shown on the website.
NEXT_PUBLIC_CONTACT_EMAIL=
